#!/usr/bin/env python
# -*- coding:utf-8 -*-
# Author: %id%, Date: %date%
from setuptools import setup, find_package


setup(
    name="package_name",
    version="1.0.0",
    description='',
    author='%id%',
    packages=find_package(),
    install_requires=[
    ],
)
